/** A single text feature card in the bento grid. Specific to this feature (ISP, §6.3-I). */
export interface FeatureItem {
  readonly title: string;
  readonly description: string;
  readonly href: string;
}
